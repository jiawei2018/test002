package laixiaobanDP;

public class maxMinCandy {

    public int MaxMin(int[] nums, int k){
        //split nums into k parts
        //try to make the smallest one as bigger as possible
        //must be adjacent
        int n = nums.length;
        int[][] dp = new int[n][k];
        //dp[i][j] means till index i and j part ,the max-min of this array




        return 0;

    }
}
